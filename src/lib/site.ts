/**
 * Canonical production origin. Every absolute URL we emit (metadata,
 * sitemap, robots, structured data, share links) must use this origin so
 * search engines never see competing hosts for the same content.
 */
export const SITE_URL = "https://milankovitchcycles.com";
